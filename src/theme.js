// Brand tokens. Same four colours as BRAND_KIT.md — deliberately not the
// purple-gradient AI look. `panel` and `line` are the two derived greys the
// card-style scenes share; they are not brand colours.
export const C = {
  void: '#0F1115',
  orange: '#FF4D2E',
  paper: '#F5F3EF',
  steel: '#8A94A6',
  panel: '#15181E',
  line: '#262B35',
};

export const FPS = 30;
export const W = 1080;
export const H = 1920;

// YouTube overlays its own UI over the bottom of a Short. Nothing that has to
// be read goes below this line.
export const SAFE_BOTTOM = 1640;

// The loop-back tail: the last half second shows the hook again so the replay
// lands on the question the video opened with.
export const TAIL_FRAMES = 15;
