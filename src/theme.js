// Brand tokens. Same four colours as BRAND_KIT.md — deliberately not the
// purple-gradient AI look.
export const C = {
  void: '#0F1115',
  orange: '#FF4D2E',
  paper: '#F5F3EF',
  steel: '#8A94A6',
};

export const FPS = 30;
export const W = 1080;
export const H = 1920;

// YouTube overlays its own UI over the bottom of a Short. Nothing that has to
// be read goes below this line.
export const SAFE_BOTTOM = 1640;
