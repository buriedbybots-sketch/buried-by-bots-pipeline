node_modules/
out/

# generated by scripts/tts.py on every run, in CI too
public/vo/
content/*.audio.json

# fetched from the sheet at build time — the sheet is the source of truth
content/20*.json

# generated by scripts/bed.py on first build; ~2.8MB of sine waves
public/music/pulse.wav

# content/protected.json IS tracked: one-way hashes of Vault 1/2/3/26, made by scripts/protect.mjs.
# Never put the prompt text itself anywhere in this repo.

# fetched from Pixabay at build time when a scene names a photo
public/photos/

# local secrets, loaded by build.mjs when present. Never tracked; CI uses GitHub Secrets.
.env
